// 3.3 Casos para las extensiones (Ternarias)
package main;

import "fmt";

func main() {
    var x, y, max int;
    x = 5;
    y = 10;

    max = (x > y) ? x : y;
    fmt.Println(max)
}

// Expected output: 10